# Privy
NEXT_PUBLIC_PRIVY_APP_ID=

# Privy (server-side only — NEVER prefix with NEXT_PUBLIC_)
PRIVY_APP_SECRET=

# Alchemy RPC
NEXT_PUBLIC_ALCHEMY_API_KEY=

# LI.FI API key — used for li.quest (Composer / quotes) and earn.li.fi (Earn Data API).
# Optional for public tiers; recommended for production rate limits.
LIFI_API_KEY=

# Set to 1 in dev to use desktop browser for QA (desktop blocker is production-only)
# NEXT_PUBLIC_DISABLE_DESKTOP_BLOCKER=1

# MoonPay (server-side only)
MOONPAY_API_KEY=
MOONPAY_SECRET_KEY=

# AI (server-side only)
# ANTHROPIC_API_KEY=
# DEEPSEEK_API_KEY=
